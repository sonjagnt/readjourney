export const selectTitle = (state) => state.filters.bookTitle;
export const selectAuthor = (state) => state.filters.author;
export const selectPages = (state) => state.filters.pages;
export const selectFilters = (state) => state.filters;
